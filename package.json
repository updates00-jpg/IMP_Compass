{
  "name": "imp-compass-api",
  "version": "1.1.0",
  "description": "IMP Compass API — Firebase FCM + Pusher (Vercel serverless)",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/blob": "^0.24.0",
    "firebase-admin": "12.1.0",
    "pusher": "5.2.0"
  },
  "scripts": {
    "lint": "node --check api/trigger-alarm.js api/send-push.js api/subscribe.js api/roster.js lib/auth.js lib/firebase-admin.js lib/send-push-logic.js sw.js",
    "check": "npm run lint && echo 'All files syntax OK'"
  }
}
