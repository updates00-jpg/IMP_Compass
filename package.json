{
  "name": "imp-compass-api",
  "version": "1.0.0",
  "description": "IMP Compass API — Firebase FCM + Pusher",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "pusher": "^5.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
